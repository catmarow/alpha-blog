<h1 class="text-center mt-4">Listing all articles</h1>

<div class="container">
<% @articles.each do |article| %>
  <div class="row justify-content-md-center">
    <div class="col-8 mt-4">
      <div class="card text-center shadow mb-5 bg-white rounded">
        <div class="card-header font-italic">
          by <%= article.user_id if article.user_id %>
        </div>
        <div class="card-body">
          <h5 class="card-title"><%= link_to article.title, article_path(article), class: "text-success" %></h5>
          <p class="card-text"><%= truncate(article.description, length: 100) %></p>

          <% if false %> THIS IS COMMENTED OUT CODE. ONLY SHOWS BUTTON WITHOUT LINKING TO ANY PAGE/FUNCTION.
          <a href="#" class="btn btn-outline-success">View</a>
          <% end %>
          <%= link_to "View", article_path(article), class: "btn btn-outline-success" %>

          <% if false %> THIS IS COMMENTED OUT CODE. ONLY SHOWS BUTTON WITHOUT LINKING TO ANY PAGE/FUNCTION.
          <a href="#" class="btn btn-outline-info">Edit</a>
          <% end %>
          <%= link_to "Edit", edit_article_path(article), class: "btn btn-outline-info" %>

          <% if false %> THIS IS COMMENTED OUT CODE. ONLY SHOWS BUTTON WITHOUT LINKING TO ANY PAGE/FUNCTION.
          <a href="#" class="btn btn-outline-danger">Delete</a>
          <% end %>
          <%= link_to "Delete", article_path(article), class: "btn btn-outline-danger", method: :delete, data: {confirm: "Are you sure?"} %>

        </div>
        <div class="card-footer text-muted">
          <small>Created <%= time_ago_in_words(article.created_at) %> ago,
          edited <%= time_ago_in_words(article.updated_at) %> ago</small>
        </div>
      </div>
    </div>
  </div>
<% end %>
</div>


<!--
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.description %></td>
        <td><%= link_to 'Show', article_path(article.id) %></td>
        <td><%= link_to 'Edit', edit_article_path(article.id) %></td>
        <td><%= link_to 'Delete', article_path(article.id), method: :delete, data: { confirm: "Are you sure?"} %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>
  <%= link_to 'Create new article', new_article_path %>
</p> -->
